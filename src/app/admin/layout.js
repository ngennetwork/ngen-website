export const metadata = {
  title: "Admin",
  description: "Internal NGEN admin tools for managing events and startups. Not for public use.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminLayout({ children }) {
  return children;
}
